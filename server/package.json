{
  "name": "pocketdesk",
  "version": "1.0.0",
  "description": "iPhone-first web remote desktop gateway (guacd + guacamole-lite)",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:vendor": "esbuild vendor/guac-entry.js --bundle --minify --global-name=Guacamole --outfile=public/vendor/guacamole.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "guacamole-lite": "^1.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "guacamole-common-js": "^1.5.0"
  }
}
